{
  "name": "mdoc",
  "version": "0.1.2",
  "scripts": {
    "start": "browser-sync start --files='index.html, css/*.css, js/*.js' --server"
  },
  "dependencies": {
    "showdown": "^2.1.0"
  },
  "devDependencies": {
    "browser-sync": "^2.7.6"
  },
  "private": true
}
